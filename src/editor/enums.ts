export enum WidgetType {
    image = 'IMAGE',
    text = 'TEXT'
}

export enum QuoteType {
    username = 'USERNAME'
}

export enum TextWidgetEvent {
    input = 'input'
}

export enum HistoryType {
    stage = 'stage',
    widget = 'widget'
}
